/**
 * KnoSys basis
 */

const DEFAULT_NAME = 'knosys';
const DEFAULT_TITLE = 'KnoSys';

const GLOBAL_DIR_NAME = `.${DEFAULT_NAME}`;
const LOCAL_DIR_NAME = `.${DEFAULT_NAME}`;
const CONFIG_FILE_NAME = `.${DEFAULT_NAME}rc`;

module.exports = { DEFAULT_NAME, DEFAULT_TITLE, GLOBAL_DIR_NAME, LOCAL_DIR_NAME, CONFIG_FILE_NAME };
